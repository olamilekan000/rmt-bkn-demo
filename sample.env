PORT=8000
# Paste the Rumpty Postgres URL as-is (it includes sslmode=require).
# Use the internal URL when the app and database are in the same workspace.
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
